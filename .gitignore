__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/
.DS_Store
.env

# Credenciais do Google Cloud — nunca commitar.
# (.gitignore não aceita comentário na mesma linha do padrão.)
*.json
!CITATION.cff

# Configuração local de ferramenta, não faz parte do pacote.
.claude/

# Microdados baixados localmente — NUNCA versionar.
# O projeto não redistribui dado algum: nem PNADC (IBGE/Base dos Dados) nem
# ACS/PUMS (o folktables baixa para data/ ao chamar get_data(download=True)).
data/
*.csv
*.zip
